package org.example;

public class GreetingUtility {
    public static void welcomeUser(String userName) { // Parameter name changed from 'name' to 'userName'
        // Print a welcome message with the user's name
        System.out.println("Hello, " + userName + "! Welcome to Java programming."); // Updated message to include "Welcome"
    }
}
